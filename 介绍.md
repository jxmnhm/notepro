# Notepro · 备忘录里的 AI 助手

> 在 macOS「备忘录」里就地调用 AI——选中文字一键处理，或输入 `/` 唤出命令菜单。结果可替换正文，也可在独立对话窗里追问、复制、插入。

![版本](https://img.shields.io/badge/version-0.7.0-blue) ![平台](https://img.shields.io/badge/macOS-13%2B-black) ![原生](https://img.shields.io/badge/Swift%20%2B%20AppKit-原生应用-orange)

---

## 它是什么

Notepro 是一个常驻菜单栏的 **原生 macOS 小应用**（Swift + AppKit 编写，不是网页套壳）。它把 DeepSeek 的 AI 能力直接嵌进系统「备忘录」的使用流里：你不用复制粘贴、不用切到别的窗口，在写笔记的当下就能润色、翻译、总结、提取待办、续写。

苹果不开放「备忘录」的界面注入，所以市面上的「Notes AI 插件」本质都是旁路方案。Notepro 用了两条都不打扰你的路径：**备忘录窗口内的浮动按钮** + **斜杠命令**，把 AI 真正放进了你的笔记现场。

---

## 三种用法

| 入口 | 怎么触发 | 适合场景 |
|------|----------|----------|
| **浮动 ✨ 按钮** | 备忘录窗口内右下角，点一下弹菜单 | 已选中一段文字，想处理它 |
| **斜杠命令** | 在备忘录里输入 `/` | 写到一半，想就当前行/续写调用 AI |
| **菜单栏图标** | 顶部菜单栏 ✨ | 改设置、查看用法 |

按钮**只贴在备忘录窗口上**，跟着它移动缩放；别的窗口盖住备忘录时，按钮也一起被盖住，绝不全局乱飘。

---

## 能做什么

- **润色文字** — 不改原意，让表达更通顺专业（直接替换正文）
- **翻译成英文 / 中文** — 自然流畅的双向翻译（直接替换正文）
- **续写** — 接着你的风格和主题往下写（直接替换正文）
- **总结要点** — 提炼成清晰要点（弹独立窗，不动正文）
- **提取待办** — 抽成 Markdown 复选框清单（弹独立窗，不动正文）

「替换型」直接改正文最顺手；「生成型」弹一个独立窗口，不污染你的笔记。

---

## 独立对话窗的亮点

生成型动作会弹出一个**可拖动、可缩放**的浮窗，它不只是只读结果框：

- ⌨️ **流式输出** — 内容像打字一样实时冒出来
- 🎨 **Markdown 渲染** — 标题、列表、任务清单、**加粗**、`代码`、引用、分隔线、**表格**都漂亮排版
- 💬 **多轮追问** — 底部输入框直接继续聊，「再精简一点」「换个语气」它都记得上下文
- 📋 **复制 / 插入正文** — 一键拿走结果，或插回备忘录光标处

---

## 隐私与权限

- **API Key 只存本机**，不上传任何第三方
- **浮动按钮的显示**不需要任何权限（靠系统窗口信息定位）
- **执行 AI 动作**需要「辅助功能」权限（用于复制选区、替换文本），仅在你触发动作时使用，且只在备忘录前台时监听 `/`
- 笔记内容只发往你配置的 DeepSeek 接口，用完即走

---

## 快速上手

1. 打开 Notepro，菜单栏出现 ✨ → 「设置…」填入 DeepSeek API Key
2. 打开「备忘录」，右下角出现浮动 ✨ 按钮
3. 选中文字点按钮，或输入 `/` 唤出菜单
4. 首次执行动作时按提示授予「辅助功能」权限

---

## 技术栈

- **语言**：Swift 6 + AppKit / SwiftUI
- **构建**：Swift Package Manager，`build.sh` 一键打包 .app
- **窗口跟随**：CGWindowList（无需权限）
- **划词执行**：CGEvent 合成 ⌘C/⌘V + NSPasteboard
- **斜杠监听**：CGEventTap（listenOnly，不拦截输入）
- **AI**：DeepSeek V4（deepseek-v4-flash / pro），SSE 流式
- **渲染**：自写轻量 Markdown → NSAttributedString（含 NSTextTable 表格）

---

## 版本更新

### v0.7.0（当前）
- 🎨 **菜单改为竖直展开**：点 ✨ 按钮，动作从按钮上方整齐弹出（胶囊条 + 图标），再点收回
- 🧩 **新建提醒事项**：待办结果可一键写入系统「提醒事项」App
- 📋 **粘贴即转清单**：复制一段 `- [ ]` / `-【】` 待办粘进备忘录，自动变原生圆圈复选框（中英文括号都认）
- ⚡️ **交互降摩擦**：没选中文字默认按全文处理（不再报错）；没填 Key 直接弹设置窗
- 🎯 **斜杠菜单跟随文字光标**：输入 `/` 时菜单在光标处弹出，不再跑到屏幕中间
- 🛠️ **修复**：浮动按钮切到别的 App 立即隐藏（不再全局悬浮）；删光自定义动作不再被默认值复活；`#` 等标记触发更稳

### v0.6.2
- 🐛 **修复 `- [ ]` 清单不触发**：补上标准 Markdown 写法（减号与方括号间带空格），现在 `- [ ] ` / `-[ ] ` / `[ ] ` 三种写法都能变原生圆圈复选框
- 🛡️ **触发更稳**：抓取选区改用轮询 + 重试，根治录屏/系统繁忙时"明明选中却报没选中"、标题偶发不触发；耗时操作移至后台线程，不再冻结界面

### v0.6.1
- 🐛 **修复实时 Markdown**：`-[ ] ` / `[ ] ` 现在能正确变原生清单（圆圈复选框），新增 `> ` 引用、` ``` ` 代码（映射到等宽字体）。之前只有标题生效的问题已解决。

### v0.6.0
- ✨ **实时 Markdown 格式化**（核心卖点）：开着 Notepro，在备忘录里行首打 `# ` / `## ` / `### ` / `- [ ] ` 加空格，**立即套用备忘录原生标题、清单格式**——所见即所得，不用碰菜单。设置里可开关。
- 📋 **「插入正文」自动转原生清单**：把 AI 生成的 `- [ ]` 待办插回备忘录时，自动转成原生圆圈复选框

### v0.5.0
- 🧩 **提示词管理**：设置里新增「提示词管理」标签，所有动作菜单可**无限自定义**——增、删、改、排序。每个动作能自定义菜单名、提示词，以及"是否弹独立窗口""斜杠时是否选全文"两个行为。不同人都能调成自己趁手的工具。

### v0.4.0
- 🐛 **修复多屏定位**：双屏/多屏下浮动按钮与对话窗会跑到另一块屏、越拖越远的问题已解决（坐标翻转改用主屏高度为基准）
- 🎯 **斜杠选区更智能**：输入 `/` 后，「提取待办」默认选中全文，其它动作默认选中**光标所在的整段**（而非只选一行）
- 🎨 **配套应用图标**，可安装到「应用程序」，启动台/聚焦搜索直接打开
- 📦 提供 `.dmg` 安装包，拖拽即装

### v0.3.x
- 💬 结果窗升级为**可拖动、可缩放的多轮对话窗**，支持就地追问（记得上下文）
- 📊 对话区 **Markdown + 表格**渲染
- 🌊 生成型动作**流式输出**
- ⌨️ 新增 **斜杠命令**：输入 `/` 唤出 AI 菜单
- 🔀 动作分流：替换型直接改正文，生成型弹独立窗口

### v0.2.x 及更早
- 📌 备忘录窗口内**浮动 ✨ 按钮** + 划词 AI（润色/翻译/总结/提取待办/续写）
- 🔑 接入 DeepSeek V4，设置内填 Key
- 🔒 稳定自签名，辅助功能授权一次长期有效

---

_Notepro v0.7.0 · 一个让 AI 住进备忘录的原生 macOS 应用_
