# Notepro 首次安装说明

> 一个住在「备忘录」里的 AI 助手。这份说明带你从零开始装好、用上，大约 3 分钟。

---

## 一、安装（拖一下就好）

1. 双击打开 **Notepro-0.4.0.dmg**
2. 在弹出的窗口里，把 **Notepro 图标** 拖到旁边的 **Applications 文件夹**
3. 打开「启动台」或按 `⌘ 空格` 搜 “Notepro”，点开它

装好后，菜单栏顶部会出现一个 ✨ 小图标——它就常驻在那里了。

---

## 二、第一次打开被系统拦住？（重要，必看）

因为这是自签名应用（没有花钱买苹果开发者证书），macOS 的 Gatekeeper 一定会在第一次拦一下。**这不是出错，按下面做一次就好，之后永久正常。**

### 最可靠的方法：右键打开（推荐发给别人时这样教）

1. 在「应用程序」里找到 Notepro，**右键点击**它 → 选 **「打开」**
2. 弹窗会变成带「打开」按钮的版本，点 **「打开」**
3. 之后就能正常双击使用了

> ⚠️ 直接**双击**会只给「移到废纸篓 / 取消」，没有打开选项——一定要用**右键 →「打开」**。

### 如果右键打开也被拦（较新的 macOS 会更严格）

提示「已损坏，无法打开」或「Apple 无法检查其是否包含恶意软件」时：

1. 打开 **系统设置 → 隐私与安全性**
2. 滚到最下面「安全性」区域，会看到一句关于 Notepro 被阻止的提示
3. 点旁边的 **「仍要打开」**，再确认一次

### 终极方法：一行命令解除限制（发给懂一点电脑的朋友）

如果上面都不行（常见于从微信/网盘下载的包，被打上了隔离标记），让对方打开「终端」粘贴这一行回车：

```bash
xattr -dr com.apple.quarantine /Applications/Notepro.app
```

这会移除系统给下载文件加的「隔离」标记，之后正常双击即可打开。

---

## 三、填入 DeepSeek API Key（必需）

Notepro 用 DeepSeek 提供 AI 能力，需要你自己的 Key：

1. 去 [platform.deepseek.com](https://platform.deepseek.com/) 注册并创建一个 API Key（形如 `sk-xxxxxx`）
2. 点菜单栏的 ✨ 图标 → **设置…**
3. 把 Key 粘进去（⌘V），选模型，点 **保存**

> 🔒 你的 Key 只保存在本机，不会上传到任何地方。

---

## 四、授予「辅助功能」权限（第一次用动作时）

Notepro 需要这个权限来「复制你选中的文字」和「把结果替换回去」。第一次执行 AI 动作时会弹窗引导：

1. 点弹窗里的 **「打开系统设置」**
2. 在 **辅助功能** 列表里，打开 **Notepro** 的开关
3. **退出并重新打开 Notepro**（菜单栏 ✨ → 退出，再重新打开）

> 只有「执行动作」需要这个权限；浮动按钮的显示本身不需要。

---

## 五、开始使用

打开「备忘录」，你有两种方式唤起 AI：

**方式 A：浮动按钮**
- 备忘录窗口右下角有个 ✨ 圆按钮
- 选中一段文字 → 点它 → 选一个动作

**方式 B：斜杠命令**
- 在备忘录里输入 `/`
- 鼠标处弹出菜单 → 选一个动作

可用动作：

| 动作 | 效果 |
|------|------|
| 润色文字 | 直接替换选中文字 |
| 翻译成英文 / 中文 | 直接替换选中文字 |
| 续写 | 接着你的内容往下写 |
| 总结要点 | 弹独立窗口，不动正文 |
| 提取待办 | 弹独立窗口，输出清单 |

总结、提取待办会弹出一个**可拖动、可缩放的对话窗**：内容流式显示、支持 Markdown 和表格，还能在底部继续追问（它记得上下文），结果可一键复制或插入正文。

---

## 常见问题

**Q：备忘录里看不到 ✨ 按钮？**
A：确认备忘录窗口在最前面。按钮只贴在备忘录上，切到别的 App 会自动隐藏。

**Q：点了动作没反应 / 提示要权限？**
A：去「系统设置 → 隐私与安全性 → 辅助功能」打开 Notepro，然后**退出重开** Notepro。

**Q：把 Notepro 的源码文件夹删了，还能用吗？**
A：能。应用是自包含的，装进「应用程序」后就和源码无关了。

**Q：怎么彻底卸载？**
A：把「应用程序」里的 Notepro 拖进废纸篓即可。如需清掉设置：删除 `~/Library/Preferences/com.floatboat.notepro.plist`。

---

## 本版更新（v0.7.0）

- 🎨 **菜单改为竖直展开**：点 ✨ 按钮，动作从按钮上方整齐弹出，再点收回
- 🧩 **新建提醒事项**：待办结果可一键写入系统「提醒事项」App
- 📋 **粘贴即转清单**：复制一段 `- [ ]` / `-【】` 待办粘进备忘录自动变圆圈复选框（中英文括号都认）
- ⚡️ **更省心**：没选中文字默认按全文处理（不再报错）；没填 Key 直接弹设置
- 🎯 **斜杠菜单跟随光标**：输入 `/` 菜单在文字光标处弹出
- 🛠️ 修复浮动按钮全局悬浮、删光动作被默认值复活、`#` 触发不稳等问题
- 📦 提供 `.dmg` 安装包（即本文件），拖拽即装

> 已装过旧版的话：菜单栏 ✨ → 退出，再用新版覆盖安装即可；辅助功能授权无需重设，自定义的提示词也会保留。

---

_Notepro v0.7.0 · 祝使用愉快_
